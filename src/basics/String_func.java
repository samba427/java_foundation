package basics;

public class String_func
{
    void main(String[] args)
    {
        String name="Samba";
        int l=name.length();
        // also have charAt, indexOf, lastIndexOf, toUpperCase
        // replace to change sm chars... isEmpty to check if empty, trim removes spaces
        // contains will check if sm is contained
        // equals and equalsIgnoreCase
        // substring(start,end) creates a substring from that start index to that end index
    }
}
